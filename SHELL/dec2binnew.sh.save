#!/bin/bash

echo Decimal number given: $1

# a ir dotais decimālais cipars
a=$1;
array=()
while [ "$a" != 0 ]
do

# array ir atlikums no dalijuma un ir massīvs; + zīme nozīmē kā notiek elementa pievienošana pie massīva

  array+=$(($a%2))
  a=$(($a/2))
done

echo ${array=$(echo "${array[@]}" | rev)
echo "Binary of $1 is:" $c

